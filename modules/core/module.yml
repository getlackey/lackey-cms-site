name : 'Lackey CMS Site'

require:
    - cms/users
    - cms/cms

init:
    Language:
        -
            code: en
            enabled: true
            default: true

    Content: !import config/content/pages.yml config/content/pages.yml

    Template: !import config/blocks.yml config/templates.yml

    User: !import config/user.yml
